bora_cumpade main()
simbora
    trem_discolhe bool_a, bool_b, bool_c uai
    
    bool_a fica_assim_entao eh uai
    bool_b fica_assim_entao num_eh uai
    
    // 'tamem' usa Python 'and'. "eh" and "num_eh" em Python são strings verdadeiras.
    // O resultado em Python será a última string ("num_eh"), e será printado.
    bool_c fica_assim_entao bool_a tamem bool_b uai
    
    oia_proce_ve("Resultado de eh tamem num_eh (deveria ser num_eh/falso): ") uai
    oia_proce_ve(bool_c) uai
    
    // No entanto, se bool_c virar True nativo do Python, ele imprime "True"
cabo
